import { widthRatio } from './styleSheet'

const setWidthRation = widthRatio * 50

const Constants = {
  TOP_PIPE_WIDTH: setWidthRation,
  BOTTOM_PIPE_WIDTH: setWidthRation
};

export default Constants
